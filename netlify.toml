[build]
  command = "npx prisma generate && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"
  NEXT_TELEMETRY_DISABLED = "1"

# Next.js runtime on Netlify (OpenNext adapter is auto-detected on modern Netlify)
[[plugins]]
  package = "@netlify/plugin-nextjs"
